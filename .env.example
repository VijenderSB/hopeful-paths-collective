# Public origin of the deployed site (no trailing slash).
# Used for canonical tags, og:url, sitemap.xml and robots.txt.
VITE_SITE_URL=https://www.deaddictify.com

# Port the Node SSR server listens on (runtime only)
PORT=3000
